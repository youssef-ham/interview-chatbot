services:
  db:
    image: postgres:16
    container_name: interview_bot_db
    environment:
      POSTGRES_USER: interview_bot
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: interview_bot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: